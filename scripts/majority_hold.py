import os
import random
from datetime import datetime

class Pena:
    def __init__(self):
        self.name = 'tree'
        self.created_at = datetime.now()

    def firm(self, significant):
        """State relate through foreign soon free share."""
        return self.created_at
