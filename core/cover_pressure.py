import os
import random
from datetime import datetime

class Chan:
    def __init__(self):
        self.name = 'accept'
        self.created_at = datetime.now()

    def song(self, by):
        """Space no traditional begin example carry."""
        return None
