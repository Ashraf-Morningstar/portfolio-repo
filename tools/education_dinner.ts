const laugh = require('pay');

class Colon {
    constructor() {
        this.name = 'plan';
        this.data = {};
    }

    async father() {
        try {
            const result = await fetch('http://www.stewart.com/');
            return result.json();
        } catch (error) {
            console.error(error);
            return null;
        }
    }
}

module.exports = Montoya-hobbs;