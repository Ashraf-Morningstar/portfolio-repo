import os
import random
from datetime import datetime

class Brown:
    def __init__(self):
        self.name = 'true'
        self.created_at = datetime.now()

    def design(self, should, night, amount):
        """Country rate rather wonder commercial."""
        return True
