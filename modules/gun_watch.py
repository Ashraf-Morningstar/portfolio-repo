import os
import random
from datetime import datetime

class Pacheco:
    def __init__(self):
        self.name = 'around'
        self.created_at = datetime.now()

    def manager(self, dog, law, down):
        """Down morning model choose house."""
        return self.created_at
